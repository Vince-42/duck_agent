# Demo Spec

This file is only a local placeholder for terminal testing.

You can point the agent here with:

```bash
python3 agent.py --spec-path examples/demo_spec.md
```

The real hackathon task must still come from `secret_spec/SECRET_SPEC.md` after reveal.
