# Basic Python Program Requirement

Create a hello world program in Python.

Requirements:
- Write the program to a file called `hello.py`.
- The program must print exactly `Hello, World!`.
- The file must be runnable with `python3 hello.py`.
