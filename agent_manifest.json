{
  "team_name": "duck_agent",
  "primary_model": "qwen2.5-coder:7b",
  "provider": "Ollama with optional Groq Cloud fallback",
  "runtime_or_tool": "custom Python agent with orchestrator-driven multi-role system",
  "additional_models": [
    {
      "model": "mixtral-8x7b-32768",
      "provider": "Groq Cloud",
      "purpose": "optional fallback provider for agent reasoning"
    }
  ],
  "paid_frontier_models_used_after_spec_release": false,
  "copilot_or_paid_ide_assistant_used_after_spec_release": false,
  "institutional_or_work_model_quota_used_after_spec_release": false,
  "model_configuration_location": "agent.py, orchestrator.py, agent.json",
  "notes": "The runtime uses one structured-cognition engine with JSON-defined mode switching layered over the existing DuckAgent execution shell. Update after the hidden task release if the setup changes."
}
